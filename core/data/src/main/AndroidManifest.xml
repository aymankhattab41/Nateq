<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
        مزوّد إشعارات داخلي بين عمليتي التطبيق (main و :tts):
        غير مُصدَّر (لا يُستدعى من تطبيقات خارجية)، وكل ما يفعلانه عبره
        هما الاشتراكُ (registerContentObserver) والإبلاغ (notifyChange)
        مروُراً بنفس الـ UID فلا حاجة لأي تصريح.
        -->
        <provider
            android:name="com.aymankhattab.nateq.core.data.SettingsChangeProvider"
            android:authorities="com.aymankhattab.nateq.settings"
            android:exported="false" />
    </application>

</manifest>