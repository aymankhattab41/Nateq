*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/app/build
# مخرجات بناء الوحدات المعيارية (البند 4): كل /build داخل أي وحدة
**/build/
/.kotlin/
/captures
.externalNativeBuild
.cxx
local.properties

# بيانات التوقيع (حسّاسة) — لا تُرفع إلى git إطلاقاً
/key/
/key.properties
*.jks
*.keystore
*.html

# ملفات تقارير وسجلات: لا تُرفع إلى git (محتواها توصية أو محلي)
*.txt
*.log

# استثناء: ملف الأداء الأساسي (بند 6#6) مصدرُه أساسي ويُلتزم رغم قاعدة *.txt
!**/baseline-prof.txt

# كاش وأسرار wrangler (Cloudflare) — لا تُرفع أبداً (قد تحوي بيانات حساب)
relay-worker/.wrangler/
